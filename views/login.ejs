<!doctype html>
<html>
  <head>
    <title>Login</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/static/css/main.css" />
    <link rel="stylesheet" href="/static/css/materialize.min.css" />
  </head>
  <body>
    <div>
      <h1 class="center-align">Login</h1>
      <h2><%= message %></h2>
      <div class="card-panel teal lighten-2">
        <p>
          If you are not currently trying to register a new device, please do
          not enter any information into the fields below and close this page.
          Someone may be trying to access your account.
        </p>
      </div>
      <div class="row">
        <div class="col m12 l6">
          <div class="z-depth-3 wrapper">
            <form action="/login" method="POST" spellcheck="false" class="row">
              <div class="input-field col s12">
                <input
                  type="text"
                  name="username"
                  placeholder="Username"
                  requried
                />
                <label for="username">Username</label>
              </div>
              <div class="input-field col s12">
                <input
                  type="password"
                  name="password"
                  placeholder="Password"
                  requried
                />
                <label for="password">Password</label>
              </div>
              <input type="hidden" name="flow_token" value="<%= flow_id %>" />
              <div class="input-field col s12">
                <input type="submit" class="btn" value="Login" />
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <script src="/static/js/materialize.min.js"></script>
    <script src="/static/js/main.js"></script>
  </body>
</html>
